/** Shim for the copied 3D scene: graphics preference types (HEREBRUM-compatible). */
export type GraphicsAdapterPreference = "system" | "low-power" | "high-performance";
export type GraphicsAnimationProfile = "balanced" | "maximum";
