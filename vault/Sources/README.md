---
title: Sources
kind: note
---
Immutable raw layer (karpathy llm-wiki pattern): drop source documents here as
markdown. The `ingest-sources` task summarizes new ones into `Agent/` via
proposals and updates [[Agent/index]]. Sources are never edited by agents.
